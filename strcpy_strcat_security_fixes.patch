--- a/src/syswatch.c
+++ b/src/syswatch.c
@@ -273,10 +273,16 @@ void cmdline(int argc, char **argv, char *conf_file, int *listenport)
 			case 'f':
 				if (x+1 == argc)
 					usage();
-				strcpy(conf_file,argv[x]+2);
-				if (strlen(conf_file) == 0)
+				/* Use snprintf to prevent buffer overflow */
+				if (strlen(argv[x]+2) > 0)
 				{
-					strcpy(conf_file,argv[x+1]);
+					snprintf(conf_file, 256, "%s", argv[x]+2);
+				}
+				else if (x+1 < argc)
+				{
+					snprintf(conf_file, 256, "%s", argv[x+1]);
+					x++;
+				}
+				else
+				{
 					x++;
 				}
 				break;
@@ -1885,7 +1891,8 @@ int main(int argc, char **argv)
 		exit(1);
 	}

-	strcpy(configfile, CFILE);
+	/* Use snprintf to prevent buffer overflow from long CFILE macro */
+	snprintf(configfile, sizeof(configfile), "%s", CFILE);

 	if (argc > 1)
 	{
--- a/src/client.c
+++ b/src/client.c
@@ -75,7 +75,11 @@ void	parse_data(char *buff, struct downdata *stuff, int bufflen)
 			switch(field)
 			{
 				case 1:
-					strcpy(stuff->hostname, space);
+					/* Use strncpy with explicit null termination */
+					strncpy(stuff->hostname, space, sizeof(stuff->hostname) - 1);
+					stuff->hostname[sizeof(stuff->hostname) - 1] = '\0';
 					break;
 				case 2:
 					stuff->type = atoi(space);
@@ -144,7 +148,8 @@ void	top_banner(char *server)
 #endif

         time (&t); /* get the time */
-        strcpy(nfo,ctime(&t)+4); /* convert it to a string, copy to buffer */
+        /* Use strftime for safer time formatting */
+        strftime(nfo, sizeof(nfo), "%b %d %H:%M:%S %Y", localtime(&t));

 #ifdef NICEINTERFACE
 	sprintf(tempbuf, "Server: %-30s%-20s%-20s", server,
@@ -285,7 +290,7 @@ void top_data(char *server)
 	char tempbuff[1024];

 	time(&now);
-	strcpy(nfo, ctime(&now)+4); /* convert it to a string */
+	strftime(nfo, sizeof(nfo), "%b %d %H:%M:%S %Y", localtime(&now));

 #ifdef NICEINTERFACE
 	sprintf(tempbuff, "Server: %-30s%-20s%-20s", server,
@@ -316,13 +321,16 @@ int main(int argc, char **argv)
 	temp = getenv("SYSMON_HOST");
 	if (temp != NULL)
 	{
-		strcpy(server, temp);
+		/* Protect against overly long environment variables */
+		snprintf(server, sizeof(server), "%s", temp);
 	}
 	switch(argc)
 	{
 		case 2:
-			strcpy(server, argv[1]);
+			/* Protect against long command-line arguments */
+			snprintf(server, sizeof(server), "%s", argv[1]);
 			break;
 		case 3:
-			strcpy(server, argv[1]);
+			snprintf(server, sizeof(server), "%s", argv[1]);
 			port = atoi(argv[2]);
 			break;
 		default:
--- a/src/srvclient.c
+++ b/src/srvclient.c
@@ -824,7 +824,8 @@ void acceptclient(int sock)
 	thisclient->filedes = msgsock; /* save accept()'ed fd */
 	thisclient->un = NULL; /* no username yet */
 	thisclient->ip = MALLOC(20, "thisclient_ip");
-	strcpy(thisclient->ip, inet_ntoa(remote.sin_addr)); /* save source ip */
+	/* Use snprintf for IP address to prevent any potential overflow */
+	snprintf(thisclient->ip, 20, "%s", inet_ntoa(remote.sin_addr));
 	thisclient->authlvl = 0; /* no auth yet */
 	thisclient->xml = FALSE;
 	thisclient->outage_log = FALSE;
--- a/src/radius.c
+++ b/src/radius.c
@@ -372,7 +372,8 @@ int radius_chk(char *hostname, int port, char *username, char *password, char *
 	packet[packetindex] = 32;
 	packetindex++;
 	packet[packetindex] = 8;
 	packetindex++;
-	strcpy(packet+packetindex, "sysmon");
+	/* Use memcpy for fixed-length string copy */
+	memcpy(packet+packetindex, "sysmon", 6);
 	packetindex += 6;

 	packet[packetindex] = 61;
--- a/src/page.c
+++ b/src/page.c
@@ -8,7 +8,8 @@

 char *translate_string(struct service *svc, char *str)
 {
-	char out[1024];
+	/* Increase buffer size to reduce overflow risk */
+	char out[4096];
 	char tmp[1024];
 	int x;
 	time_t t;
@@ -53,6 +54,9 @@ char *translate_string(struct service *svc, char *str)
 		sprintf(out, "translate_string() error with %s", svc->hostname);
 		return strdup(out);
 	}
+
+	/* Track current string length to prevent overflow */
+	size_t current_len = 0;

         /* convert to page */
         for (x=0; x < strlen(str);x++)
@@ -65,10 +69,18 @@ char *translate_string(struct service *svc, char *str)
 			switch(str[x])
 			{
 				case 'n':
-					strcat(out, "\n");
+					if (current_len + 1 < sizeof(out)) {
+						strncat(out, "\n", sizeof(out) - current_len - 1);
+						current_len++;
+					}
 					break;
 				case 'r':
-					strcat(out, "\r");
+					if (current_len + 1 < sizeof(out)) {
+						strncat(out, "\r", sizeof(out) - current_len - 1);
+						current_len++;
+					}
 					break;
 			}
 			continue;
@@ -78,48 +90,84 @@ char *translate_string(struct service *svc, char *str)
                         switch (str[x])
                         {
                                 case 'm':
-                                        strcat(out, myhostname);
+					if (current_len + strlen(myhostname) < sizeof(out)) {
+						strncat(out, myhostname, sizeof(out) - current_len - 1);
+						current_len += strlen(myhostname);
+					}
                                         break;
 				case 'H':
-					strcat(out, get_hostname(hp));
+					{
+						const char *hostname = get_hostname(hp);
+						size_t len = strlen(hostname);
+						if (current_len + len < sizeof(out)) {
+							strncat(out, hostname, sizeof(out) - current_len - 1);
+							current_len += len;
+						}
+					}
 					break;
                                 case 'd': /* downtime */
-                                        strcat(out, str_difftime(svc->deathtime,t));
+					{
+						char *difftime_str = str_difftime(svc->deathtime,t);
+						size_t len = strlen(difftime_str);
+						if (current_len + len < sizeof(out)) {
+							strncat(out, difftime_str, sizeof(out) - current_len - 1);
+							current_len += len;
+						}
+					}
                                         break;
                                 case 'l': /* uptime */
-                                        strcat(out, str_difftime(svc->last_up,t));
+					{
+						char *uptime_str = str_difftime(svc->last_up,t);
+						size_t len = strlen(uptime_str);
+						if (current_len + len < sizeof(out)) {
+							strncat(out, uptime_str, sizeof(out) - current_len - 1);
+							current_len += len;
+						}
+					}
                                         break;
                                 case 'D': /* downtime 2 */
-                                        strcat(out, str_difftime_sec(svc->deathtime, t));
+					{
+						char *downtime_str = str_difftime_sec(svc->deathtime, t);
+						size_t len = strlen(downtime_str);
+						if (current_len + len < sizeof(out)) {
+							strncat(out, downtime_str, sizeof(out) - current_len - 1);
+							current_len += len;
+						}
+					}
                                         break;
 				/* ... (similar pattern for other cases) ... */
                                 case 's':
-                                        strcat(out, type_to_name(svc->type));
+					{
+						const char *type_name = type_to_name(svc->type);
+						size_t len = strlen(type_name);
+						if (current_len + len < sizeof(out)) {
+							strncat(out, type_name, sizeof(out) - current_len - 1);
+							current_len += len;
+						}
+					}
                                         break;
 				case 'T':
-					strcat(out, ctime(&t)+11);
-					out[strlen(out)-6]='\0';
+					{
+						char time_str[16];
+						strftime(time_str, sizeof(time_str), "%H:%M:%S", localtime(&t));
+						size_t len = strlen(time_str);
+						if (current_len + len < sizeof(out)) {
+							strncat(out, time_str, sizeof(out) - current_len - 1);
+							current_len += len;
+						}
+					}
 					break;
                                 case 't':
-                                        strcat(out, ctime(&t)+4);
-                                        out[strlen(out)-6]='\0';
-                                        /* time */
+					{
+						char date_str[32];
+						strftime(date_str, sizeof(date_str), "%b %d %H:%M:%S", localtime(&t));
+						size_t len = strlen(date_str);
+						if (current_len + len < sizeof(out)) {
+							strncat(out, date_str, sizeof(out) - current_len - 1);
+							current_len += len;
+						}
+					}
                                         break;
                                 case 'U':
-					strcat(out, svc->lastcheck ?
-					  "down" : "up");
+					{
+						const char *status = svc->lastcheck ? "down" : "up";
+						size_t len = strlen(status);
+						if (current_len + len < sizeof(out)) {
+							strncat(out, status, sizeof(out) - current_len - 1);
+							current_len += len;
+						}
+					}
 					break;
                                 case 'u':
-                                        strcat(out, errtostr(svc->lastcheck));
+					{
+						const char *err_str = errtostr(svc->lastcheck);
+						size_t len = strlen(err_str);
+						if (current_len + len < sizeof(out)) {
+							strncat(out, err_str, sizeof(out) - current_len - 1);
+							current_len += len;
+						}
+					}
                                         break;
                                 case 'w':
-                                        strcat(out, svc->message);
+					if (svc->message) {
+						size_t len = strlen(svc->message);
+						if (current_len + len < sizeof(out)) {
+							strncat(out, svc->message, sizeof(out) - current_len - 1);
+							current_len += len;
+						}
+					}
                                         break;
                                 case 'h':
-                                        strcat(out, svc->hostname);
+					{
+						size_t len = strlen(svc->hostname);
+						if (current_len + len < sizeof(out)) {
+							strncat(out, svc->hostname, sizeof(out) - current_len - 1);
+							current_len += len;
+						}
+					}
                                         break;
                         }
                 } else {
-                        strncat(out, str+x, 1);
+			if (current_len + 1 < sizeof(out)) {
+				strncat(out, str+x, 1);
+				current_len++;
+			}
 		}
         }

--- a/src/loadconfig.c
+++ b/src/loadconfig.c
@@ -133,6 +133,7 @@ char *replace_variable(char *string)
 	int x=0, y=0;
 	char buff[MAX_STRLEN];
 	unsigned char new_string[MAX_STRLEN];
+	size_t current_len = 0;
 	char *repl = NULL;

 	memset(new_string, 0, MAX_STRLEN);
@@ -154,12 +155,19 @@ char *replace_variable(char *string)
 			repl = find_value(buff);
 			if (repl != NULL)
 			{
-				strcat(new_string, repl);
+				size_t repl_len = strlen(repl);
+				if (current_len + repl_len < MAX_STRLEN) {
+					strncat(new_string, repl, MAX_STRLEN - current_len - 1);
+					current_len += repl_len;
+				}
 				x = (y-1);
 			}
 		} else {
-			strncat(new_string, string+x, 1);
+			if (current_len + 1 < MAX_STRLEN) {
+				strncat(new_string, string+x, 1);
+				current_len++;
+			}
 		}
 	}
 	if (repl != NULL)
